# Quarto output directories
_book/
_site/
.quarto/

# Quarto per-file build artifacts
*_files/
*.html
# ...except the meta-note toggle, which is a real site feature (must deploy)
!_meta-toggle.html

# Archive (old working files, superseded by index.qmd)
archive/

# Agent workspace
agent_workspace/

/.quarto/
**/*.quarto_ipynb
.agent